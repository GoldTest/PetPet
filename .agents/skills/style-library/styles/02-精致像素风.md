---
画风名: 精致像素风 REFINED PIXEL ART
来源: 用户需求（20260801 登记），活力果实图标验证通过（20260801_193524）
状态: 已验证（20260801，活力果实 icon_skill_fruit 评审通过）
---

# 精致像素风（REFINED PIXEL ART）

物品/道具/图标类精致像素风。与「真实纹理像素」的粗颗粒复古感相反——颗粒细腻、色块分层、高光聚焦，像素感优先、细节克制。

## 风格段（固定，直接使用）

```
**风格：REFINED PIXEL ART 精致像素风——像素颗粒明显但细腻，轻微抗锯齿过渡，轮廓干净利落，每个颜色区域分 2-3 层明暗色块（深色底→主色→高光点），高光清晰聚焦、阴影层次简单分明，色块干净不糊。像素感优先，细节点到为止，不做微观细节堆砌。不是粗颗粒复古像素，不是扁平卡通，不是写实。**
```

## 要求段（按场景微调）

```
**要求：**
- [对象细节要求，如：主体占画面 70% 以上、居中构图]
- bright magenta background (#FF00FF) 品红纯色背景（用于后续抠图）
- 主体颜色避免品红/粉紫系（高光、光晕、纹路也不要用接近品红的浅粉色，否则抠图会被误删）
- refined pixel art, crisp clean edges, flat color blocks with 2-3 shade steps, clear focused highlights, simple clear shading, pixel aesthetic first
- no text, no numbers, no labels
- 尺寸按场景选择：单物体 2048x2048 (1:1)；多对象水平拼图 3072x1376 (21:9)
```

## 完整模板

```
用 SenseNova 文生图生成一张"精致像素风"图标：[物品名]。居中构图，比例 1:1（尺寸 2048x2048）。

[风格段]

**内容：** [物品描述——主体、颜色分区、高光位置、点缀装饰，细节克制]。

[要求段]
```

## 生成要点（内容段写法）

- 优先写清"大色块分层"：主体色 + 暗部 + 高光，每个区域 2-3 层即可，不写复杂纹路
- 高光位置明确（如左上光源），光晕/发光元素收紧在主体轮廓内或紧贴边缘，不做大范围渐变光晕
- 装饰细节最多列 1-2 项（如果蒂、叶子），不逐项堆砌，避免丢失像素感
- 强调"像素感优先、细节克制"

## 参考历史（完整内容段见 `.remember/`）

- 活力果实（20260801_193524 → icon_skill_fruit.png）：紫蓝色魔法果实，三层明暗色块 + 左上高光 + 绿色卷曲果蒂 + 紧贴轮廓淡紫光晕；首个 v1（20260801_193211）因细节堆砌+抠图空心被否，此版按减细节修正后通过
